<% if object && object.errors.any? %>
  <div id="error_explanation">
    <h2><%= local_assigns[:title] || "Foram encontrados alguns problemas:" %></h2>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
