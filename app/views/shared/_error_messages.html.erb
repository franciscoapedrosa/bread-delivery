<% if object && object.errors.any? %>
  <div id="error_explanation">
    <h2><%= local_assigns[:title] || "There were some problems:" %></h2>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
